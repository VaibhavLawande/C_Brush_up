#include<stdio.h>
enum days{sun,mon};//declear 1 enum with sun,mon
enum week{wed,mon};//declear 2 enum with wed ,mon
int main()          //it will show the error because of same variable(redecleartion error)
{
}
